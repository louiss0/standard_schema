name: standard_schema
description: A type-safe Dart contract for interoperable validation libraries, based on Standard Schema V1.
version: 1.0.0

environment:
  sdk: ^3.0.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
